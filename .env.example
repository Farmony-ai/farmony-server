# Environment Configuration
NODE_ENV=development
PORT=3000

# ============================================
# DATABASE - MongoDB (Docker)
# ============================================
# For local development with docker-compose
MONGO_URI=mongodb://admin:password@localhost:27017/farmony?authSource=admin

# ============================================
# FIREBASE ADMIN SDK
# ============================================
# Option 1: Use service account JSON file (recommended for local dev)
FIREBASE_SERVICE_ACCOUNT_PATH=./config/firebase-service-account.json

# ============================================
# Firebase Storage
# ============================================
FIREBASE_STORAGE_BUCKET=your-project-id.appspot.com
FIREBASE_STORAGE_MAKE_PUBLIC=true
# If true, generate signed URLs when requesting access URLs (useful if bucket is private)
FIREBASE_STORAGE_SIGNED_URLS=false

# ============================================
# Feature Flags
# ============================================
# Set to true to enable new matching architecture (Phase 1 refactor)
MATCHING_V2_ENABLED=false
# Set to true to use MatchesOrchestratorService for acceptance
MATCHING_V2_ACCEPT_ENABLED=false
FIREBASE_SERVICE_ACCOUNT_PATH=./config/serviceaccountkey.json